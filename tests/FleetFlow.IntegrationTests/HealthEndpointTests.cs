namespace FleetFlow.IntegrationTests;

public class HealthEndpointTests
{
    [Fact]
    public void PlaceholderTest_ConfirmsProjectIsConfigured()
    {
        // Full WebApplicationFactory-based integration tests (e.g. GET /health) will be added
        // once Testcontainers-backed SQL Server is introduced.
        Assert.True(true);
    }
}

